# Machine Learning Models Directory

This directory contains:

- Trained personality prediction models
- Model artifacts and metadata
- Checkpoint files for model training

## Structure

- `trained/` - Production-ready models
- `experimental/` - Research and experimental models
- `metadata/` - Model metadata and performance metrics
